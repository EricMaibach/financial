# Core
pandas>=2.0.0
xlrd>=2.0.0
numpy>=1.24.0
requests>=2.31.0
yfinance>=0.2.0

# Flask and extensions
flask>=3.0.0
flask-sqlalchemy>=3.1.0
flask-migrate>=4.0.0
flask-login>=0.6.0
flask-wtf>=1.2.0
flask-limiter>=3.5.0
flask-mail==0.9.1

# Security
cryptography>=41.0.0
email-validator>=2.0.0

# AI
openai>=1.0.0
anthropic>=0.40.0

# Scheduling
apscheduler>=3.10.0
pytz>=2024.1
tzdata>=2024.1

# Machine Learning (regime classification)
scikit-learn>=1.3.0

# NLP pipeline for sector management tone (US-123.1)
# WARNING: torch adds ~250MB (CPU-only) to the Docker image; FinBERT model
# adds ~500MB on first download. Run update_sector_management_tone() as a
# quarterly batch job rather than on every web request.
# For CPU-only inference: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.30.0
torch>=2.0.0
